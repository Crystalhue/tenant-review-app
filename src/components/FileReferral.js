import React from 'react';
import { View, Text } from 'react-native';
import styles from '../../styles';

const FileReferral = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>File Referral Screen</Text>
    </View>
  );
};

export default FileReferral;
